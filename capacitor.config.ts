import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Herbal Home Remedies',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
